import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles((theme) => ({
    mainDiv: {
        dislay: 'flex',
        width: '100%',
        alignItems: 'center',
        marginTop: 20
      
    }
}));

export { useStyles };
